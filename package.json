{
  "name": "csboard-extension-prod",
  "version": "1.0.3",
  "description": "CSBOARD price-overlay browser extension. Read-only price feeds for Steam Community pages and CSFloat.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && node build.mjs",
    "build:fast": "node build.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.27.4",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@csfloat/cs2-inspect-serializer": "^1.1.6",
    "csgo-fade-percentage-calculator": "^2.0.0"
  }
}
