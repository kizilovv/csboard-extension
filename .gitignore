node_modules/
node_modules 2/
build/
# Same package as build/, plus the `key` lifted from the installed store copy
# so an unpacked load keeps the real extension id. Local testing only.
build-dev/
dist/
artifacts/build-meta/
artifacts/e2e/
artifacts/build-profile.json
artifacts/releases/
.git.broken/
.swarm
.swarm/
*.log
.DS_Store

# Distribution archives — built artifact, regenerated by `npm run build && zip`
csboard-extension-*.zip

# IDE / OS
.idea/
.vscode/
*.swp

# Env / secrets
.env
.env.*
*.serviceaccount.json
